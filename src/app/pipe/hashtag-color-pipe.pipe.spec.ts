import { HashtagColorPipePipe } from './hashtag-color-pipe.pipe';

describe('HashtagColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HashtagColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
